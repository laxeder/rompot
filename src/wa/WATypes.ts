import { WAChat, WAUser } from "./WAModules";

/** * Lista de salas de bate-papos */
export declare type WAChats = { [id: string]: WAChat };

/** * Lista de salas de usuários */
export declare type WAUsers = { [id: string]: WAUser };
